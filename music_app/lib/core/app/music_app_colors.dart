import 'package:flutter/material.dart';

class MusicAppColors {
  MusicAppColors._();

  // Black (preto)
  static Color get primaryColor => Color(0xff000000);

  //White (branco)
  static Color get secondaryColor => Color(0xffffffff);

  // Green (verder)
  static Color get tertiaryColor => Color(0xff779933);

  // Red (vermelho)
  static Color get errorColor => Color(0xffdc3545);
}
